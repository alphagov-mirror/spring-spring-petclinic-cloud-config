spring:
  profiles: cloud
  application:
    name: api-gateway
    
eureka:
  instance:
    nonSecurePort: 80
    securePort: 443
    hostname: ${vcap.application.uris[0]}
  client:
    service-url:
      defaultZone: ${vcap.services.discovery-server-si.credentials.uri}
